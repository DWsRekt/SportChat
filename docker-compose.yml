version: "3"
services:
  server:
    image: server
    ports:
      - "5000:5000"
    depends_on:
      - mongodb

  client:
    image: client
    stdin_open: true
    ports:
      - "3000:3000"

  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
